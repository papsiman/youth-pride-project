services:
  db:
    image: postgres:15-alpine
    container_name: pride-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: pridedb
    ports:
      - "5435:5432" # External port changed to 5435
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgbouncer:
    image: edoburu/pgbouncer
    container_name: pride-pgbouncer
    restart: always
    environment:
      DATABASE_URL: "postgresql://postgres:password123@db:5432/pridedb"
      MAX_CLIENT_CONN: 10000
      DEFAULT_POOL_SIZE: 50
      MIN_POOL_SIZE: 20
      RESERVE_POOL_SIZE: 10
      POOL_MODE: transaction # สำคัญมากสำหรับ High TPS
    ports:
      - "6432:6432"
    depends_on:
      - db

  pgadmin:
    image: dpage/pgadmin4
    container_name: pride-pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8080:80"
    depends_on:
      - db

volumes:
  postgres_data:
